const Header = () => {
  return (
    <div
      style={{
        padding: 15,
        display: "flex",
        justifyContent: "space-between"
      }}
    >
      <button>임시</button>
      <button>임시</button>
    </div>
  );
};

export default Header;
